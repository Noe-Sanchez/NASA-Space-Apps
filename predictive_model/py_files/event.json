{
  "bucket_name": "my-example-bucket",
  "prefix": "",
  "region_name": "us-east-1"
  /* Option A: For ENV test - no other fields needed */
  /* Option B: To use Secrets Manager (preferred for production)
     "secrets_manager_name": "my/s3/temp-credentials"
  */
  /* Option C: To use original OAuth flow (if you use EDS/Earthdata)
     "s3_endpoint": "https://example-s3-endpoint/",
     "edl_username": "YOUR_EDL_USERNAME",
     "edl_password": "YOUR_EDL_PASSWORD"
  */
}
